apiVersion: v1
data: {}
kind: Secret
metadata:
  annotations: {}
  labels:
    app.kubernetes.io/instance: nextcloud
    app.kubernetes.io/managed-by: commodore
    app.kubernetes.io/name: nextcloud
    name: nextcloud
  name: nextcloud
  namespace: syn-nextcloud
stringData:
  nextcloud-password: SUPER_PASSWORD
  nextcloud-token: SUPER_TOKEN
  nextcloud-username: admin
  postgres-password: SUPER_PASSWORD
  postgres-username: nextcloud
  redis-password: SUPER_PASSWORD
type: Opaque
