parameters:
  nextcloud:
    =_metadata: {}

    namespace: syn-nextcloud

    images:
      nextcloud:
        registry: docker.io
        repository: linuxserver/nextcloud
        tag: 27.0.2

    replicaCount: 1

    resources: {}
